# Copie para deploy.env (não versionado) e ajuste.
# Depois, no PC: ./scripts/deploy.sh

DEPLOY_REMOTE=root@187.127.49.235
DEPLOY_SSH_KEY=~/.ssh/id_rsa
DEPLOY_DIR=/var/www/SistemaIndiceCartorio
DEPLOY_SSH_PORT=22
